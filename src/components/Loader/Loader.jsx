import React from 'react';

import { StyledLoader, WrapperLoader } from './Styled';

function Loader() {
  return (
    <WrapperLoader>
      <StyledLoader />
    </WrapperLoader>
  );
}

export default Loader;
